<%= form_with(url: search_path, method: :get, local: true) do |f| %>
  <%= f.select :musical_instrument, Profile.musical_instruments.keys, {prompt: '選択してください'}, {class: 'search'} %>
  <%= f.submit '検索する', class: 'btn-search'%>
<% end %>