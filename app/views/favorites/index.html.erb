<div class="container">
  <div class="fovarites">
    <h2 class="fovarites_item">いいね一覧</h2>
      <% @articles.each do |article|%>
        <%= render 'commons/article', article: article %>
      <% end %>
  </div>
</div>